# Chapter 20: Observability Foundations – Spring Boot profile snippet

management:
  endpoints
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when_authorized

  metrics:
    tags:
      application: ${spring.application.name:orders-service}

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    com.ndurai: DEBUG

# Prometheus scrape endpoint
server:
  port: 8080
